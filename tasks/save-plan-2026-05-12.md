# Save Plan - 2026-05-12

Назначение: определить, что можно и нужно сохранять в первую очередь после stabilization checkpoint.

Важно: это не commit и не публикация. Это карта безопасного порядка сохранения.

## Что нужно сохранить первым

Первая партия: safety / navigation / project memory.

Эти файлы нужны, чтобы Codex и Claude Code одинаково понимали состояние проекта:

- `.gitignore`;
- `.claude/rules/site.md`;
- `.claude/rules/agents-backend.md`;
- `.claude/rules/content.md`;
- `.claude/rules/data-reports.md`;
- `.claude/rules/security.md`;
- `AGENTS.md`;
- `CLAUDE.md`;
- `README.md`;
- `.env.example`;
- `docs/secret-handling-policy.md`;
- `docs/README.md`;
- `docs/claude-project-rules.md`;
- `docs/claude-content-strategy.md`;
- `docs/claude-agent-architecture.md`;
- `research/README.md`;
- `content/README.md`;
- `data/README.md`;
- `data/reports/README.md`;
- `tasks/roadmap.md`;
- `tasks/stabilization-checkpoint-2026-05-12.md`;
- `tasks/final-stabilization-checkpoint-2026-05-12.md`;
- `tasks/save-plan-2026-05-12.md`.
- `tasks/safe-save-batch-1-2026-05-12.md`.

Важно: GitHub-репозиторий `kasatkur-dot/leadsystem` публичный, поэтому `REPORT.md` не включать в публичную save/commit партию. `REPORT.md` остаётся локальной памятью Codex/Claude и защищается через `.gitignore`.

В `.gitignore` обязательно сохранять защиту локальных Claude Code файлов:

```gitignore
CLAUDE.local.md
.claude/settings.local.json
```

Почему это первое:

- фиксирует правила проекта;
- защищает секреты и runtime-файлы;
- сохраняет текущую точку синхронизации Codex/Claude через безопасные правила и checkpoint, без публикации внутреннего `REPORT.md`;
- не запускает внешние интеграции;
- не публикует сайт и контент;
- снижает риск потерять контекст.

## Что можно сохранить после первой партии

Вторая партия: site.

Сохранять после финального ручного просмотра:

- `site/`;
- связанные site-документы в `docs/`;
- локальные безопасные визуалы;
- `OPEN_SITE.command`, если нужен удобный локальный запуск сайта.

Третья партия: docs / research / content.

Сохранять после быстрой проверки чувствительности:

- `docs/`;
- `research/`;
- `content/`;
- `content/pipeline/published-log.md`;
- `ТЗ_СЛИЯНИЕ_КОНТЕНТ_ДВИЖКА.md`, если принимаем его как актуальное ТЗ по Agent 4 Publisher.

Четвертая партия: dashboard / reports.

Сохранять:

- `data/reports/agent_dashboard.json`;
- `data/reports/agent_dashboard.md`;
- `data/reports/agent_dashboard.html`;
- dashboard build scripts.

Не сохранять:

- `data/reports/bitrix_close_test_lead_*.json`.

Пятая партия: backend / agents.

Сохранять только блоками:

- `shared/`, `config/`, `orchestrator/`;
- Agent 1/2/3;
- Agent 5;
- Agent 4 Publisher;
- Agent 6 Outreach;
- scripts.

## Что оставить локально или отложить

- `MCP_SETUP_RESULT.md` - полезный локальный отчет по настройке GitHub/Vercel/MCP, но содержит идентификаторы аккаунта. Решение: оставить локально и игнорировать через `.gitignore`.
- `data/reports/bitrix_close_test_lead_*.json` - оставить локально; это operational CRM test artifacts с реальными Bitrix lead ID и следами фактического тестового обновления лида.
- `tasks/session-notes.md` - полезный общий журнал, но большой и содержит отдельные контактные/идентификационные упоминания; перед внешней публикацией редактировать.
- `output/`, `.playwright-cli/`, `.playwright-mcp/`, `logs/`, `*.session`, `.env`, `.venv` - не сохранять.

## Принятые решения

- `ТЗ_СЛИЯНИЕ_КОНТЕНТ_ДВИЖКА.md` считать актуальным проектным ТЗ по Agent 4 Publisher. Перед сохранением прямой email в контактном блоке заменён на безопасную формулировку.
- `MCP_SETUP_RESULT.md` не сохранять в репозитории: оставить локально, потому что там есть аккаунты и идентификаторы подключений.
- `OPEN_SITE.command` можно сохранять вместе с блоком `site`: это безопасный launcher, он не содержит секретов.
- Главный checkpoint: `tasks/stabilization-checkpoint-2026-05-12.md`.
- `tasks/final-stabilization-checkpoint-2026-05-12.md` оставить как историческую раннюю сводку; не использовать как главный источник решений.

## Лучший следующий шаг

Сохранить первую партию `safety / navigation / project memory`, но не делать commit/deploy, пока пользователь явно не подтвердит.

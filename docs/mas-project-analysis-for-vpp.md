# MAS Project Analysis For VPP

Дата: 2026-05-16

Источник: `/Users/yanika/Documents/МАС`

Статус: анализ правильного проекта `МАС`. Файлы проекта `МАС` только читались, не изменялись. Внешние сервисы, API, LLM, Bitrix24, реклама, MAX, Telegram, Redis, scheduler и платные сервисы не запускались.

## 1. Что это за проект

`МАС` - это локальный MVP многоагентной системы:

```text
пользователь пишет задачу
-> оркестратор выбирает отделы и агентов
-> система строит маршрут
-> каждому агенту назначается артефакт
-> создаются Markdown/HTML-файлы
-> задача сохраняется в журнал
-> frontend показывает карту, чат, сценарии и результаты
```

Технически найдено:

- frontend: React + Vite, файл `frontend/src/main.jsx`;
- backend: FastAPI, файл `backend/app/main.py`;
- данные агентов: `backend/data/agents.json`;
- отделы: `backend/data/departments.json`;
- сценарии: `backend/data/scenarios.json`;
- маршрутизация моделей: `backend/data/models.json`;
- growth playbooks: `backend/data/growth_playbooks.json`;
- артефакты: `backend/artifacts/<task_id>/`;
- журнал задач: `backend/data/task_log.jsonl`;
- Supabase-схема: `deploy/supabase/schema.sql`;
- документы: `docs/architecture.md`, `docs/growth_engine.md`, `docs/lesson_telemost_findings.md`, `docs/honest_status.md`, `docs/agent_security.md`.

## 2. Что особенно ценно для нашего проекта

### 2.1. Не просто чат, а операционный центр

В `МАС` пользователь видит:

- карту отделов;
- список агентов;
- сценарии;
- инспектор роли агента;
- рабочий prompt;
- чат;
- журнал задач;
- созданные артефакты.

Для ВПП это полезнее обычного чата. Нам тоже нужен центр управления:

```text
источники -> лиды -> обработка -> Bitrix24 -> контент -> отчеты -> следующий шаг
```

### 2.2. Артефакт на каждом этапе

В `МАС` каждый агент выдает конкретный файл:

- Brief;
- маркетинговый план;
- HTML-прототип лендинга;
- контент-завод;
- рекламные связки;
- воронка лидов и CRM;
- ТЗ деплоя;
- метрики;
- Launch Runbook.

Для ВПП нужно так же, но по проектной организации:

- `Client Intake Brief`;
- `Source Card`;
- `Data Completeness Check`;
- `Project Scope Draft`;
- `Bitrix Deal Preview`;
- `KP Readiness Note`;
- `Content/Event Card`;
- `CRM Hygiene Report`;
- `ROMI Report`.

### 2.3. Growth engine из урока

В `МАС` зафиксирована методика из урока:

```text
идея продукта
-> MVP / оффер
-> лендинг
-> контент-завод
-> рекламные связки
-> переходы
-> заявка / регистрация
-> CRM / база
-> аналитика
-> доработка или масштабирование
```

Для ВПП адаптация такая:

```text
сегмент услуги
-> понятный оффер
-> посадочная страница или пост
-> контент доверия
-> входящее обращение
-> AI-менеджер собирает исходные данные
-> Bitrix24 сделка
-> КП / звонок / инженерная проверка
-> договор / отказ / отложено
-> отчет по источнику
```

### 2.4. Role card и prompt каждого агента

В `МАС` роль агента считается нормальной, если есть:

- миссия;
- правила;
- инструменты;
- KPI;
- пример задачи;
- рабочий prompt.

У нас это нужно применить не как 27 новых агентов, а как паспорт для текущих 6 агентов.

Пример:

```text
Agent 5 CRM
миссия: Bitrix24, сделки, дубли, SLA, ROMI
правила: не создавать сделки без валидации, не раскрывать секреты, писать источник
инструменты: Bitrix24, dashboard, duplicate audit
KPI: полнота полей, скорость обработки, меньше дублей
пример: принять входящий запрос и завести корректную сделку
```

### 2.5. Security и честный статус

В `МАС` хорошо зафиксированы правила:

- секреты не писать в документы и артефакты;
- перед production нужен API token;
- нужен rate limit;
- нужен журнал действий;
- нужна база;
- нужны права доступа;
- MVP честно не выдается за полноценный боевой продукт.

Это полностью совпадает с нашим подходом: сначала dry-run, потом один тест, потом масштабирование.

## 3. Что нельзя копировать напрямую

Не переносим в наш проект как есть:

- 27 агентов вместо наших 6;
- универсальные отделы `marketing/design/sales/development/osint` как замену текущей архитектуре;
- placeholder-артефакты вместо реальной CRM/Bitrix24-логики;
- обязательную форму заявки на лендинге;
- открытые API без нормальной защиты;
- отсутствие реальных workers/очередей;
- хранение важных данных только в JSONL и папке артефактов;
- шаблонный HTML-лендинг без бренда ВПП;
- обещания роста подписчиков или выручки как гарантию.

## 4. Как встроить это в нашу систему

### 4.1. Оставить 6 агентов, но добавить паспорта

Нам не нужен перенос 27 ролей. Нужно усилить 6 текущих агентов:

| Наш агент | Что взять из `МАС` |
|---|---|
| Agent 1 Scout | mission/rules/tools/KPI, market risk artifact |
| Agent 2 Collector | source card artifact, правила источников |
| Agent 3 Processor | data completeness, segment, scoring artifact |
| Agent 4 Publisher | content factory, landing artifact, approval gate |
| Agent 5 CRM | Bitrix deal preview, CRM hygiene, ROMI artifact |
| Agent 6 Outreach | approval, first-touch rules, no spam |

### 4.2. Добавить сценарии как маршруты

Не новые агенты, а маршруты:

| Сценарий ВПП | Маршрут |
|---|---|
| B2B-заявка на склад/ангар | Agent 4 -> Agent 3 -> Agent 5 -> человек |
| Коммерческая перепланировка | Agent 4/6 -> Agent 3 -> Agent 5 |
| Перепланировка квартиры | Agent 4/2/6 -> Agent 3 -> Agent 5 |
| Тендер | Agent 2 -> Agent 3 -> Agent 5 |
| Контентная цепочка | Agent 1 -> Agent 4 -> Agent 5 analytics |
| CRM-гигиена | Agent 5 |

### 4.3. Добавить артефакты как обязательный результат

Для каждого входящего обращения система должна сохранять:

```text
source_card
intake_card
qualified_lead
bitrix_deal_preview
missing_data_note
next_action
```

Для каждого контентного действия:

```text
content_event
offer_id
funnel_arc
cta_level
source/result metrics
```

### 4.4. Перенести growth engine в проект ВПП

Для каждого направления:

```text
направление
-> оффер
-> лендинг/страница
-> контентная цепочка
-> источник заявки
-> Bitrix24
-> аналитика
```

Это уже начато в `docs/full-offer-and-landing-matrix.md`.

## 5. Что нужно внедрить в ближайший технический шаг

Минимально и правильно:

1. Добавить в локальную логику Agent 3/AI-manager признаки:
   - `segment`;
   - `client_type`;
   - `data_completeness`;
   - `missing_data`;
   - `next_action`.
2. Сделать 3 dry-run сценария:
   - склад/ангар;
   - коммерческая перепланировка;
   - перепланировка квартиры.
3. Каждый dry-run должен создать артефакты:
   - `intake_card`;
   - `qualified_lead`;
   - `bitrix_deal_preview`;
   - `next_action`.
4. Dashboard должен показать:
   - сколько обращений по сегментам;
   - где данных хватает для КП;
   - где нужно запросить документы;
   - какие источники дают полезные обращения.

Дополнительный слой по росту заказчиков описан в `docs/mas-lesson-growth-mechanics-for-vpp.md`: как адаптировать из `МАС` механику `MVP -> контент-завод -> реклама -> заявки`, пример цели `8000 подписчиков` и продуктовый подход AI-астропсихолога без копирования обещаний и тематики.

## 6. Что сделать позже

После dry-run и проверки:

- визуальная карта 6 агентов в нашем dashboard;
- role-card для каждого агента ВПП;
- API `/health` для нашего проекта;
- OpenAPI только локально или за авторизацией;
- Supabase/PostgreSQL как будущий слой хранения, если текущих JSON-отчетов станет мало;
- настоящие LLM-вызовы только после лимитов стоимости и правил секретов;
- deploy только после проверки `.env`, Redis, Telegram-сессий, Bitrix24 и безопасности.

## 7. Вывод

`МАС` полезен не как готовый код, который надо скопировать, а как модель операционного центра.

Главное, что нужно забрать в ВПП:

```text
карта агентов
-> сценарии
-> артефакт на каждом шаге
-> growth route
-> журнал задач
-> честный статус
-> безопасность
-> аналитика результата
```

В нашем проекте это должно работать вокруг реальных направлений ВПП и Bitrix24, а не вокруг универсальной демонстрационной системы.
